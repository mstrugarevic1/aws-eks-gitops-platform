# Minimal Helm values for bootstrapping ArgoCD.
#
# ArgoCD is installed outside GitOps because it must exist before it can
# reconcile this repository. After this bootstrap step, ArgoCD manages the
# Kubernetes add-ons and workloads defined under gitops/.
