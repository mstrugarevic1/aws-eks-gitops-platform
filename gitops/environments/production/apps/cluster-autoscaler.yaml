apiVersion: "argoproj.io/v1alpha1"
kind: "Application"
metadata:
  name: "production-cluster-autoscaler"
  namespace: "argocd"
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
  finalizers:
    - "resources-finalizer.argocd.argoproj.io"
spec:
  project: "default"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: "kube-system"
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
  sources:
    -
      repoURL: "https://kubernetes.github.io/autoscaler"
      chart: "cluster-autoscaler"
      targetRevision: "9.58.0"
      helm:
        valueFiles:
          - "$values/gitops/environments/production/values/cluster-autoscaler-values.yaml"
    -
      repoURL: "git@github.com:mstrugarevic1/eks-argocd-platform-boilerplate.git"
      targetRevision: "main"
      ref: "values"
